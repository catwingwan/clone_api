services:
  # ============================
  # Backend (API) Service
  # ============================
  - type: web
    name: my-clone-api
    env: docker
    plan: free
    region: singapore
    rootDir: api
    dockerfilePath: Dockerfile
    autoDeploy: true

  # ============================
  # Frontend (Vite Web App)
  # ============================
  - type: web
    name: my-clone-web
    env: docker
    plan: free
    region: singapore
    rootDir: web
    dockerfilePath: Dockerfile
    autoDeploy: true
    # For frontend you must also set routes
    buildCommand: ""
    startCommand: ""
